const { createSqliteAdapter } = require('./src/lib/data/adapters/sqlite/index');
// The adapter requires a DB client. We can just test the error by sending the request to the Next.js API again but we need to see the server console logs.
