# Diagnostics Scripts

This folder holds one-off local diagnostics and API probes that are not part of
the supported app test suite.

Use the formal commands for routine verification:

- `npm run lint`
- `npm run test`
- `npm run build`

Most files here are exploratory checks for specific charting or API issues and
should be treated as manual debugging helpers, not production tests.
